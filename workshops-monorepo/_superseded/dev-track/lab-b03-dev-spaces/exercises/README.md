Exercise files for "Cloud Development with OpenShift Dev Spaces".

- `devfile.yaml` — the environment spec for a Dev Spaces workspace running the
  `hello-dcs` sample app (the same app from A01/B01/B02): a Harbor-mirrored dev
  image, the app source, and the command that runs it. You'll read this file on
  the next page, then use it to walk through launching and developing in a
  workspace.
