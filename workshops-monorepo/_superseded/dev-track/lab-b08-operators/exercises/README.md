Exercise files for the Operators on DCS workshop.

`sample-cr.yaml` is the minimal Custom Resource you apply on page 03 — a tiny
CloudNativePG `Cluster` with a single instance and the smallest storage, its
image resolved from the DCS Harbor registry at apply time.
