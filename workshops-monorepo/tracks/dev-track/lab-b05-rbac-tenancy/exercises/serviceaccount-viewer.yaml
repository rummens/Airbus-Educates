# A non-human subject. We bind permissions to this ServiceAccount rather than to your
# own user so you can prove an *exact* set of granted permissions with `--as`, independent
# of whatever broader access your own user already has in this namespace.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: viewer-bot
