Exercise files for "From Docker to Kubernetes on DCS".

`docker-compose.yml` is the source of truth you're migrating — the app as you'd
run it today with `docker compose up`. `deployment.yaml`, `service.yaml` and
`configmap.yaml` are the Kubernetes manifests you complete and apply as you
translate it, object by object.
