# Track metadata → one Track CR. `id` is EXPLICIT (not the folder name), so the
# folder can be renamed freely. A workshop joins this track with label
# academy.dcs/track: <id>. Only `id` + `title` are required; the rest have
# defaults (order 100, icon "layers").
id: build            # track id — must equal the workshops' academy.dcs/track label
title: "Build & Run — Your App on DCS"
description: "Build your image on the cluster, get it into Harbor, then make the app healthy, scalable and resilient."
order: 20            # after the core foundations + console tracks
icon: package        # FA-style icon name the portal vendors

# Not published yet: the labs are still being authored. The catalog greys the track
# out and refuses to start a session. Flip to `available` when they are ready.
availability: disabled
