The "source" for this lab. A real application would have code here; the shape of the
build is the same either way — a Containerfile, some files, and a builder that turns
them into an image inside the cluster.
