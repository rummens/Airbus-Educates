#!/bin/bash
# verify-whoami — the session has an authenticated identity.
who=$(oc whoami 2>/dev/null)
if [ -z "$who" ]; then
  echo "oc whoami returned nothing — no authenticated identity in this session." >&2
  exit 1
fi
echo "OK: authenticated as '$who'."
