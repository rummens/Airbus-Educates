# Exercise files — RBAC Deep Dive

- `role-viewer.yaml` — a namespace Role granting read-only access to Pods and Services.
- `rolebinding-viewer.yaml` — a ServiceAccount plus a RoleBinding that grants it the role.
