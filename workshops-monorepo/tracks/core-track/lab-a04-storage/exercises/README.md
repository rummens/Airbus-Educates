Exercise files for "Storage".

- `pvc-file.yaml`             — a PersistentVolumeClaim for File (RWX) storage.
- `hello-dcs-with-volume.yaml`— the hello-dcs app with the volume mounted at /data.
- `pvc-block.yaml`           — a PVC for Block (RWO) storage (the end-of-lab challenge).
