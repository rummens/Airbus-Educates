Exercise files for "Expose Your App".

- `deployment.yaml` — the hello-dcs app in UI mode (serves HTML and prints its own URL).
- `service.yaml`    — a Service giving the app a stable in-cluster address.
- `route.yaml`      — an OpenShift Route exposing the app externally on DCS-managed DNS.
