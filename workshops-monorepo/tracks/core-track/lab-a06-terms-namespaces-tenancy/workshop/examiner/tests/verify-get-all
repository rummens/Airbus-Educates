#!/bin/bash
# Verify 'oc get all' runs against the active namespace.
if ! oc get all >/dev/null 2>&1; then
  echo "'oc get all' failed against '$(oc project -q 2>/dev/null)'." >&2
  exit 1
fi
echo "OK: 'oc get all' ran against '$(oc project -q 2>/dev/null)'."
