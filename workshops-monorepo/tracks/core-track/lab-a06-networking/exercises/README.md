# Exercise files — Networking & Exposing Apps

- `hello-dcs.yaml` — the sample Deployment + Service (from earlier workshops).
- `route.yaml` — an OpenShift Route exposing the Service. Its host must include the session hostname (Kyverno enforces this on DCS).
