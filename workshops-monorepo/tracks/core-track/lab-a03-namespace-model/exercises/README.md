# Exercise files — Namespaces & the Prod/Dev Model

- `dev-namespace.yaml` — a DEV-type namespace.
- `prod-namespace.yaml` — a PROD-type namespace (Kyverno policies apply here).
- `hello-dcs.yaml` — the sample Deployment + Service you deploy into each namespace.
- `kyverno-policy.yaml` — a representative Kyverno policy that applies only to PROD-type namespaces.
