Exercise files for "Deploy Your First App".

In this lab you *create* the Deployment with `oc` — there's no manifest to start from.
On the last page you save the generated YAML here (`deployment.yaml`) and read it, to see
the desired-state document `oc` produced for you.
