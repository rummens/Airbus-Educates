# A Service in front of the app, so there is something for a Route to point at.
apiVersion: v1
kind: Service
metadata:
  name: hello-dcs
  labels:
    app: hello-dcs
spec:
  selector:
    app: hello-dcs
  ports:
  - name: http
    port: 8080
    targetPort: 8080
