# Exercise files — Operators on DCS

- `sample-cr.yaml` — a minimal CloudNativePG `Cluster` Custom Resource. Applying it asks the
  CloudNativePG operator to create and manage a small PostgreSQL cluster for you. You own
  this instance; the platform owns the operator.
