#!/bin/bash
# verify-clusterroles — cluster roles are listable (read access to cluster-scoped RBAC).
if ! oc get clusterroles >/dev/null 2>&1; then
  echo "Could not list ClusterRoles. RBAC read access may be missing in this session." >&2
  exit 1
fi
echo "OK: ClusterRoles are listable."
