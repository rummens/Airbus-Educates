# Per-cluster values for the educates-workshops chart (CRC / OpenShift Local).
ingressDomain: apps-crc.testing
auth:
  enabled: true
  existingSecret: educates-oauth-proxy
  tlsCertificateRef:
    name: router-certs-default
    namespace: openshift-ingress
vcluster:
  enabled: true
